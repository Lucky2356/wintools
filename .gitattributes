# Байты этих файлов важны: .cmd требуют CRLF (иначе ломается разбор batch),
# menu.cmd и descr.ru лежат в CP866. Любая нормализация переводов строки или
# перекодировка сделает их нерабочими, поэтому конвертация выключена целиком.
* -text
